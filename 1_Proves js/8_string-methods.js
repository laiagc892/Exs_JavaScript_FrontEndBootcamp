// Exercici nom i cognoms. S'introdueix una string de tres paraules i les torna per separat 
        // amb la primera lletra en majúscules
        
        var nom1, nom2, nom3;
        var nom = "Un cotxe vermell" // prompt("Introdueix el nom i els cognoms separats per espais")
        
        // aquí el teu codi;

        // Usant el mètode split                                // COPIAR TOT L'EXERCICI DELS DE L'OMAR (inici classe dilluns 21/12, no m'he empanat de res d'això)
        // arr = nom.split(" ");
        // nom1 = arr[0];
        // nom2 = arr[1];
        // nom3 = arr[2];

        // trobar el primer espai
        var primerEspai = nom.index0f(" ");
        // trencar lacadena per allà
        nom1 = nom.substring(0,)
        
        console.log({"nom": nom1, "primer cognom": nom2, "segon cognom": nom3});
        
        // -------------------------------------------------------------------------------------------------